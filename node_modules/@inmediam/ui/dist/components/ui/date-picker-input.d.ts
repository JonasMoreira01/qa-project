import * as react_jsx_runtime from 'react/jsx-runtime';

interface DatePickerInputProps {
    label?: string;
    placeholder?: string;
    /** Valor inicial no formato dd/MM/yyyy */
    defaultValue?: string;
    /** Chamado sempre que o valor muda. `date` é undefined se a data for inválida */
    onChange?: (date: Date | undefined, masked: string) => void;
}
declare function DatePickerInput({ label, placeholder, defaultValue, onChange, }: DatePickerInputProps): react_jsx_runtime.JSX.Element;

export { DatePickerInput, type DatePickerInputProps };
